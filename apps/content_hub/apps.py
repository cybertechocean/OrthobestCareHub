from django.apps import AppConfig

class ContentHubConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.content_hub'
    verbose_name = 'Content Hub (Blog, FAQs, Services & Policies)'
